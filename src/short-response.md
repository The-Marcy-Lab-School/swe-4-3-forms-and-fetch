# Short Response Questions

## Directions

Answer the questions below using complete sentences. Code snippets or examples can help illustrate your answers. Each question is worth 1 point for a total of 6 points.

## Question 1: `event.preventDefault()`

When handling form submissions with JavaScript, why do we need to call `event.preventDefault()`? What happens if we forget to include it?

**Your Answer:**


## Question 2: Extracting Form Data

Compare the two methods for extracting form data: `form.elements` and the FormData API. Describe one advantage and one disadvantage of each approach.

**Your Answer:**


## Question 3: Checkbox Handling with FormData

When using the FormData API with checkboxes, what value does a checked checkbox return? What value does an unchecked checkbox return? How do we convert these values to proper boolean values (`true`/`false`)?

**Your Answer:**


## Question 4: Promise Chaining

When working with Promises (like those returned by `fetch()`), why is it important to `return` a Promise from inside a `.then()` callback? What happens if you forget to return it?

**Your Answer:**


## Question 5: HTTP Methods

Describe the purpose of each of the following HTTP methods and give an example of when you would use each:
- GET
- POST
- PATCH
- DELETE

**Your Answer:**


## Question 6: Error Handling with fetch()

When using `fetch()`, there are two main types of errors that can occur. The first is when the fetch request itself fails (network error, malformed URL). The second is when the request succeeds but the server responds with an error status (like 404 or 500). How do you handle each of these error types? Provide code examples.

**Your Answer:**

